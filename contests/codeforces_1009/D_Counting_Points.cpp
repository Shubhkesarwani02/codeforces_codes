#include <bits/stdc++.h>
#define int long long
#define vi vector<int>
#define input(v)      \
    for (auto &i : v) \
    cin >> i
#define output(v)     \
    for (auto &i : v) \
    cout << i << " "
#define pb push_back
#define all(a) a.begin(), a.end()
using namespace std;

void helper()
{
    int n, m;
    cin >> n >> m;
    vi centers(n), radii(n);
    input(centers);
    input(radii);

    

    
}

signed main()
{
    int t;
    cin >> t;
    while (t--)
    {
        helper();
    }
    return 0;
}
